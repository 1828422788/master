[parse_chinese_char_maxmatch100_exceed_rawlen]
note = v2.4，默认max_match超过配匹值个数时，取最大匹配个数
query = tag:sample04061424_chinese_5 | limit 1 | parse field=raw_message "(?<first_chinese_char>[\u4e00-\u9fa5])" max_match=100 | table first_chinese_char
source_groups = all_
time_range = now/d,now
total = 72

[eval_match_query]
note = Describe later 
query =  tag:"sample04061424" AND apache.request_query:* | eval r_match1 = match(apache.request_query, "b.*e") | eval r_match2 = match(apache.request_query, "^bcd") | table apache.request_query, r_match1, r_match2, apache.x_forward | sort by  apache.x_forward 
source_groups = all_
time_range = now/d,now
total = 72

[index_yotta_regex]
note = Describe later；高亮功能；下载功能
query = index=yotta tag:sample04061424 AND  /[0][8]0{2,2}/
source_groups = all_
time_range = now/d,now
total = 72

[tran_ip_maxopen_max]
note = 共返回5组，组内日志数最多为10；下载功能
query = (logtype:apache AND tag:sample04061424) | transaction apache.clientip maxopenevents=10 maxevents=10
source_groups = all_
time_range = now/d,now
total = 5
local_skip_keys_list = _duration

[tran_apachelen_sort]
note = 共返回5组，组内日志数最多为10；下载功能
query = tag:"sample04061424" | transaction  apache.resp_len | sort by apache.resp_len
source_groups = all_
time_range = now/d,now
total = 29
local_skip_keys_list = _duration

[tran_count_where]
note = Describe later；下载功能
query = tag:"sample04061424" | transaction apache.resp_len | where _count==4
source_groups = all_
time_range = now/d,now
total = 72
local_skip_keys_list = _duration

[bug_tran_with]
note = bug followed；下载功能
query = tag:t_with | transaction json.sid with states a, b, c in json.module results by flow
source_groups = all_
time_range = now/d,now
total = 72
local_skip_keys_list = _duration